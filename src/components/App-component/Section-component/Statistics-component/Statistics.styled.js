import styled from 'styled-components';

export const StatisticsTextItem = styled.li`
  font-family: Arial, Helvetica, sans-serif;
  font-size: 24px;
  font-weight: 500;
  margin-bottom: 10px;

  &:last-child {
    margin-bottom: 0;
  }
`;

export const AccentText = styled.span`
  color: #8b7aa8;
  font-weight: 600;
`;

export const StatisticList = styled.ul`
  padding: 30px;
`;
